<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:tint="@color/loginEle">
    <corners android:radius="15dp" />
    <gradient
        android:centerColor="@color/loginEle"
        android:endColor="@color/loginEle"
        android:startColor="@color/loginEle" />
    <solid android:color="@color/loginEle" />

</shape>